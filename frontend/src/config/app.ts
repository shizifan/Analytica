/**
 * App-level constants — product decisions, not deployment configuration.
 *
 * For deployment-time configuration (API base URL, etc.), use Vite env vars
 * (`import.meta.env.VITE_*`) instead.
 */

export const DEFAULT_EMPLOYEE_ID = 'asset_investment';
